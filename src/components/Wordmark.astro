---
/*
 * Wordmark — `oriz` set in display serif (Source Serif 4) with an
 * international-orange hairline strike through the `z` glyph — the
 * proofreader's deletion mark rendered as the brand mark.
 *
 * The z-strike is the family anchor (kept across sites) but each site owns
 * its type/palette: on oriz-blog the wordmark is serif with the signal-orange
 * strike; sister sites render their own identity.
 */

interface Props {
  /** Sub-section name — rendered after a `/` separator. */
  subtitle?: string
  /** Display size in CSS pixels. */
  size?: number
  /** Wrap the mark in an <a href="/"> — defaults to '/' when subtitle is set. */
  href?: string | null
  /** Render as <h1> (the masthead) rather than <div>. */
  asHeading?: boolean
}

const { subtitle, size = 96, href = null, asHeading = false } = Astro.props
const Tag = asHeading ? 'h1' : ('div' as const)
---

{
  href ? (
    <a class="wordmark-link" href={href} aria-label={subtitle ? `oriz / ${subtitle}` : 'oriz'}>
      <Tag class="wordmark" style={`--wm-size: ${size}px;`}>
        <span class="wordmark-mark">
          <span class="wordmark-letter">o</span>
          <span class="wordmark-letter">r</span>
          <span class="wordmark-letter">i</span>
          <span class="wordmark-letter wordmark-z">
            z
            <span class="wordmark-strike" aria-hidden="true" />
          </span>
        </span>
        {subtitle && (
          <>
            <span class="wordmark-sep" aria-hidden="true">
              /
            </span>
            <span class="wordmark-sub">{subtitle}</span>
          </>
        )}
      </Tag>
    </a>
  ) : (
    <Tag class="wordmark" style={`--wm-size: ${size}px;`}>
      <span class="wordmark-mark">
        <span class="wordmark-letter">o</span>
        <span class="wordmark-letter">r</span>
        <span class="wordmark-letter">i</span>
        <span class="wordmark-letter wordmark-z">
          z
          <span class="wordmark-strike" aria-hidden="true" />
        </span>
      </span>
      {subtitle && (
        <>
          <span class="wordmark-sep" aria-hidden="true">
            /
          </span>
          <span class="wordmark-sub">{subtitle}</span>
        </>
      )}
    </Tag>
  )
}

<style>
  .wordmark-link {
    display: inline-block;
    color: inherit;
    text-decoration: none;
  }
  .wordmark {
    display: inline-flex;
    align-items: baseline;
    gap: 0.18em;
    margin: 0;
    font-family: var(--font-display);
    font-weight: 600;
    font-size: var(--wm-size, 96px);
    line-height: 1;
    letter-spacing: -0.03em;
    color: var(--ink);
  }
  .wordmark-mark {
    display: inline-flex;
    align-items: baseline;
  }
  .wordmark-letter {
    position: relative;
    display: inline-block;
  }
  .wordmark-z {
    padding-inline: 0.15em;
  }
  .wordmark-strike {
    position: absolute;
    inset: 50% 0 auto 0;
    height: 0.055em;
    background: var(--cobalt);
    transform: translateY(-50%) rotate(-2deg);
    pointer-events: none;
  }
  @media (max-width: 480px) {
    .wordmark-strike {
      height: max(1px, 0.04em);
    }
  }
  .wordmark-sep {
    margin-inline: 0.2em;
    color: var(--ink-mute);
    font-weight: 400;
  }
  .wordmark-sub {
    color: var(--cobalt);
    font-family: var(--font-mono);
    font-weight: 500;
    letter-spacing: 0.02em;
  }
</style>
